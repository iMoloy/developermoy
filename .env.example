# ── Root .env.example ─────────────────────────────────────────
# Copy to .env and populate values before running.
# Each workspace also has its own .env.example

# See client/.env.example for client-side variables
# See server/.env.example for server-side variables
